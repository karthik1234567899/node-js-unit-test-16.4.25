{
  "devDependencies": {
    "mocha": "^9.0.0",
    "chai": "^4.3.4",
    "sinon": "^11.0.0",
    "mocha-junit-reporter": "^2.0.0"
  },
  "scripts": {
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/test-results.xml"
  }
}
